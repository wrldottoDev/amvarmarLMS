# AMVARMAR LMS — backend runtime dependencies
# Stack: FastAPI async + SQLAlchemy 2.x async + PostgreSQL (asyncpg) + Redis + Celery
# Ref: documentation/AMVARMAR_LMS_Arquitectura_Tecnica_v1.md (secciones 2.2, 4, 7)
# Python >= 3.12

# --- API framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.18          # uploads multipart/form-data

# --- Configuración validada ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0

# --- Base de datos ---
sqlalchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0
psycopg[binary]>=3.2,<4.0         # solo para scripts/migrate_legacy.py y utilidades sync

# --- Seguridad: Argon2id + JWT ---
pwdlib[argon2]>=0.2.1,<1.0        # hashing Argon2id (recomendado por FastAPI)
argon2-cffi>=23.1,<26.0
pyjwt[crypto]>=2.10,<3.0          # access/refresh JWT firmados
cryptography>=43.0                # HMAC-SHA-256 fingerprint, cifrado tokens FCM

# --- Redis: caché de permisos, rate limiting, broker ---
redis[hiredis]>=5.2,<6.0

# --- Worker / outbox / tareas asíncronas ---
celery[redis]>=5.4,<6.0
tenacity>=9.0,<10.0               # reintentos outbox / proveedores externos

# --- Object storage S3-compatible (MinIO en local) ---
boto3>=1.35,<2.0
botocore>=1.35,<2.0

# --- Notificaciones ---
firebase-admin>=6.5,<7.0          # FCM push
aiosmtplib>=3.0,<4.0              # envío de correo async
jinja2>=3.1,<4.0                  # plantillas de correo

# --- Validación de archivos subidos ---
python-magic>=0.4.27,<1.0         # MIME real, no confiar en el header del cliente
pillow>=11.0,<12.0                # validación/normalización de imágenes

# --- Cliente HTTP ---
httpx>=0.28,<1.0

# --- Observabilidad ---
structlog>=24.4,<26.0             # logs JSON con request_id, actor, ruta, status
prometheus-fastapi-instrumentator>=7.0,<8.0
opentelemetry-sdk>=1.28,<2.0
opentelemetry-instrumentation-fastapi>=0.49b0
opentelemetry-instrumentation-sqlalchemy>=0.49b0
opentelemetry-exporter-otlp>=1.28,<2.0
sentry-sdk[fastapi]>=2.18,<3.0

# --- Utilidades ---
orjson>=3.10,<4.0
python-dateutil>=2.9,<3.0
